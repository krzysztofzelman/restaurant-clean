"""
Warehouse business logic.

Currently handled directly in the API layer since operations are
straightforward CRUD. Add complex logic here (e.g. batch FIFO
allocation, low-stock alerts) as the module grows.
"""
